{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and focus timer popup",
  "windows": ["main", "focus-popup"],
  "permissions": [
    "core:default",
    "opener:default",
    "notification:default"
  ]
}
